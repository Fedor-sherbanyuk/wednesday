# getCheckingWithRegexDataUser
# The first function checks the first name using which he entered, using a regular expression.
# The second function checks the last name using which he entered, using a regular expression.
# The third function checks the user's mail, which he entered, using a regular expression
# The fourth function checks the user's password, which he entered, using a regular expression.
